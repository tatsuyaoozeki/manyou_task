<!-- <h3>以下の内容で、送信する。</h3>

<%= form_with(model: @task, url: tasks_path, local: true) do |form| %>
  <%= form.hidden_field :name %>
  <%= form.hidden_field :content %>

  <p>タイトル:<%= @task.name %></p>
  <p>本文:<%= @task.content %></p>
  <%= form.submit  "登録する", name: 'back' %>
<% end %> -->
